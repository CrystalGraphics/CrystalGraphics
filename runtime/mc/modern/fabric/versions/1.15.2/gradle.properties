# fabric @ 1.15.2 -- Loom, Mojang's mappings and no Parchment (it starts at 1.16.5). Also claims 1.15 and
# 1.15.1: fabric-api 0.28.5 is the 1.15.2 backport and declares ~1.15.
mc.version = 1.15.2
fabric.loader = 0.15.11
fabric.api = 0.28.5+1.15

# The variant this node ships: the Minecraft range it CLAIMS -- the bootstrapper refuses, by name, any
# version no node claims -- and that Minecraft's resource-pack format.
variant.minecraft = [1.15,1.16)
variant.packFormat = 5

# Fabric API for 1.15 has no WorldRenderEvents, so the world passes are a mixin, gated to this node.
variant.mixinPlugin = com.crystalgraphics.mc.shared.CrystalGraphicsFabricMixins
